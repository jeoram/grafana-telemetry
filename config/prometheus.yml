global:
  scrape_interval: 1s
  evaluation_interval: 1s

scrape_configs:
  - job_name: 'cpp_trading_engine'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['cpp-engine:8080']
        labels:
          application: 'trading_telemetry_engine'
          environment: 'production'
